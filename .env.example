# Copie este arquivo para .env e ajuste o valor antes de rodar o Docker.
#
# Se o backend roda direto no seu PC (fora do Docker):
API_BASE=http://firmato-api:8000
#
# Se o backend também está em um container Docker na mesma rede:
# API_BASE=http://firmato-api:8000
#
# Se o backend está em outro servidor ou cloud:
# API_BASE=https://api.seudominio.com
